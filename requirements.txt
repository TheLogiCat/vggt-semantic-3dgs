# Core dependencies (mirrors VGGT requirements)
torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24.0
Pillow
einops

# VGGT base model (install from GitHub or PyPI)
# Uncomment the line that matches your setup:
# Option A – install from GitHub (recommended for latest code)
# vggt @ git+https://github.com/facebookresearch/vggt.git
# Option B – install as editable local clone
# -e /path/to/vggt

# Model hub / checkpoint download
huggingface_hub
safetensors

# Optional: DINOv2 via transformers (for real SemanticTokenizer backbone)
# transformers>=4.38.0

# Development / testing
pytest
